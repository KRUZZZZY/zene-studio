# advertised-features.tsv — the ONE list of features this product's releases
# document as present, and the build options that must hold for that claim to be
# true. tests/release-honesty-gate.sh reads this file and nothing else: the gate
# carries no feature list of its own, so there is no second copy to drift from
# the release notes.
#
# WHAT THIS FILE EXISTS TO PREVENT (kept from the v0.1.0-alpha defect, because it
# is the reason the gate exists): the published v0.1.0-alpha advertised "VST3 and
# CLAP hosting, effects only" while all seven jobs of its release run printed
# "VST3 hosting skipped: no SDK" / "CLAP hosting skipped: no headers" and shipped
# neither. Both options default to AUTO, and AUTO with the dependency absent is a
# skip with a STATUS line — a silent compile-out that no test noticed.
#
# CHANGED FOR 0.2.0-alpha — the rows below are the 0.2.0 contract:
#   * vst3-instrument-hosting is NEW. The host module is plugins/Vst3Instrument,
#     whose target is BUILD_PLUGIN(vst3instrument ...) and which is gated by
#     WANT_VST3 — the SAME option the effect host uses, which the release jobs
#     already pass as -DWANT_VST3=ON. It is deliberately NOT gated by
#     WANT_VST3_TEST_INSTRUMENT: that option (default OFF) builds the MIT
#     test-instrument FIXTURE, a test asset, and is a row of its own below so the
#     two cannot be confused in either direction.
#   * the 0.2.0 release notes must therefore describe instrument hosting with the
#     limits we actually measured: one instrument per track, MIDI in to audio out;
#     no third-party instrument has been run (the witness is the in-tree MIT
#     fixture); the plugin's own editor does not open, so its parameters surface
#     as a generated grid; no multi-out, no presets, no instrument latency in PDC,
#     no out-of-process hosting.
#
# A NOTE ON WHY `session-view` MOVED FROM required=OFF TO required=ON IN 0.3.0-alpha.
# Until 0.3.0 this row read OFF, because `OPTION(WANT_SESSION_VIEW "..." OFF)` defaulted OFF and no
# release job passed it: the Session View data layer, its launch scheduler and the session.* control
# group existed in the source and shipped in no build. The row was the thing that made the flip
# non-optional (a build that turned the option on failed this gate until the documents moved), and it
# worked - 0.3.0-alpha is an engine-and-agent release, so the option now defaults ON, the release jobs
# get it without passing anything, and this row requires ON with the same commit flipping the default.
#
# WHAT THE ROW PROTECTS NOW, stated as narrowly as it can be, because it is the difference between a
# claim and a wish. What this build CONTAINS with the option ON: the data layer (include/SessionModel.h,
# the versioned <session> project block), the launch scheduler wired into Song::processNextBuffer
# (src/core/SessionScheduler.cpp), and the session.* command group (src/core/ControlCommandsSession*.cpp)
# through which an agent builds the grid, launches a clip or a whole scene, and reads the launch back.
# What it still does NOT contain, and what the release notes must therefore not claim: there is NO CLIP
# LAUNCHER - no grid, no scene launcher, no clip-cell UI, no Follow Action editor - and a launched
# session slot does not render audio, because this tree has no session-clip playback path. A user cannot
# see or hear the Session View; an agent can drive all of it through --control-socket. That sentence, not
# the option's value, is the honest description of 0.3.0-alpha's Session View.
#
# WHY THERE IS NO ROW FOR TELEMETRY, THOUGH THE RELEASE DOCUMENTS IT.
# The gate can only judge what the binary REPORTS, and the build-options string is assembled by
# src/CMakeLists.txt from variables matching `^WANT|LMMS_(HAVE|DEBUG)`. The telemetry kill switch is
# `ZENE_TELEMETRY` (default ON) and it matches neither pattern, so it never reaches `lmms --version` —
# and a row for an option the build does not report is a FAIL by this gate's own rule. Two ways to close
# it, both post-release: expose a `LMMS_HAVE_TELEMETRY` flag into the dump (which also lets a user tell
# whether their binary contains the client), or widen the CMake pattern. Until then telemetry is
# documented in prose and guarded by its own tests, not by this contract.
# WHY THERE IS NO ROW FOR THE VST3 TEST-INSTRUMENT FIXTURE — and the reason is NOT what this note first said.
# The first version claimed the option "is declared inside tests/CMakeLists.txt under a condition, so it never
# reaches the generated header". A merge train proved that FALSE on 2026-09-12: its tree's `build/lmmsversion.h`
# reports `WANT_VST3_TEST_INSTRUMENT='OFF'`. The real mechanism is subtler and worse. The dump is assembled in
# `src/CMakeLists.txt` through `get_cmake_property(_define_vars VARIABLES)`, which runs BEFORE
# `ADD_SUBDIRECTORY(tests)` — so on a FRESH configure the option does not exist yet and is absent from the dump;
# but `option()` writes a CACHE variable, so on any later configure of the same build directory it is already
# defined at that point and DOES land in the dump. Consequence: **the same source tree can export two different
# `LMMS_BUILD_OPTIONS` strings depending on the build directory's history.** A row for this option would
# therefore pass or fail according to how many times a directory had been configured, which is worse than having
# no row — the guard's job is to describe the build, not the build directory's biography. The fixture is a test
# asset documented in prose; that is the whole claim.
# GENERAL CAVEAT THIS EXPOSES, which belongs with the guard rather than only here: the dump is a function of the
# source tree AND the build directory's configure history. All six rows below use options declared at the TOP
# LEVEL of `CMakeLists.txt`, so they are present either way — but any future row for a nested or conditionally
# declared option must be verified on a FRESH configure before it is trusted.
# Columns (TAB separated):
#   1 feature   stable id, printed by the gate
#   2 option    the WANT_* option the binary reports in its "Build options:" dump
#   3 required  the value that must hold: ON, OFF, or an exact string
#   4 module    plugin module basename to find in the build tree ('-' for none)
#   5 claim     the sentence this row keeps true, quoted when the gate fails
#   6 platforms where the claim holds: '*' (or empty) for every platform, or a comma
#               list drawn from linux,macos,windows. On a platform NOT listed here the
#               gate asserts the INVERSE — the option must not be ON and the module
#               must not exist under --artifacts — so a platform is never silently
#               unchecked; see the platform section in tests/release-honesty-gate.sh.
#
# WHY clap-hosting IS `*` AGAIN (2026-09-13). The 0.2.1 patch scoped this row to
# linux,macos because plugins/ClapEffect/ClapHost.cpp loaded its plugins with
# dlopen/dlsym (<dlfcn.h>), which neither MinGW nor MSVC provides — the v0.2.1-alpha tag
# run was the first Windows build ever to reach the file, and it failed there:
#   plugins/ClapEffect/ClapHost.cpp:34:10: fatal error: dlfcn.h: No such file or directory
# The loader now has a Windows branch behind `#ifdef _WIN32` (LoadLibraryW/GetProcAddress/
# FreeLibrary, opened through the wide path so a non-ASCII install directory works) and
# all three Windows jobs pass -DWANT_CLAP=ON, so the claim holds on every platform and
# this row says `*`. The port itself is proven by the three Windows CI jobs, NOT on the
# development box (no MinGW or MSVC toolchain is installed there): until a Windows job
# runs green, the Windows half is unverified, and the honest reading of this row is
# "the documents claim it everywhere; CI is what proves the Windows claim."
#
# 2026-09-15 (task #667) - the row is unchanged; two notes were added, because the loader
# it names now says WHICH failure it had as well as that it failed. The codes and their
# stable tokens live in plugins/ClapEffect/ClapLoader.h, which is also where the platform
# boundary and the load ladder moved to (it is the only file in the tree that looks up
# `clap_entry`), and ClapLoaderErrorTest drives them against four modules built to fail in
# exactly one way each (tests/data/clap-test-plugin/clap-test-broken.c). The CI side of
# this row did not change: all three Windows jobs still pass -DWANT_CLAP=ON, and of those
# three the msvc-x64 job is the one that runs ctest, so it is where that test - and the
# Windows half of the loader - is exercised. Nothing here is claimed as locally verified:
# see tests/prove-clap-loader-unchanged.sh for exactly what the Linux box does prove.
#
# A feature documented as ABSENT is a row too (required OFF). If a build turns one
# on, the documents understate that build and the release is dishonest in the other
# direction, so the gate fails on it as well.
vst3-hosting	WANT_VST3	ON	vst3effect	VST3 hosting (effects) is in the release	*
vst3-instrument-hosting	WANT_VST3	ON	vst3instrument	VST3 instrument hosting (one instrument per track, MIDI in to audio out) is in the release	*
clap-hosting	WANT_CLAP	ON	clapeffect	CLAP hosting (effects) is in the release on every platform	*
session-view	WANT_SESSION_VIEW	ON	-	The Session View data layer, its launch scheduler and the session.* control group are in the release builds. The claim this row keeps true is the narrow one: it is drivable and observable through --control-socket, and there is still NO clip-launch grid and NO scene-launcher UI, and a launched session slot does not render audio (docs/KNOWN-LIMITATIONS.md)	*
wasm-sandbox	WANT_WASM	OFF	-	The WASM DSP sandbox is documented as absent from these builds	*
stem-separation	WANT_STEM_SPLIT	OFF	-	Offline stem separation is documented as absent from these builds	*
