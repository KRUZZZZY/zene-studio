{
  "id": "transport/tempo-map-0001",
  "scope": "transport",
  "seed": 5000,
  "groups": [
    "transport"
  ],
  "steps": [
    {
      "cmd": "transport.get_state",
      "expect": {
        "paused": false,
        "playing": false
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.tempo_map_get",
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.tempo_map_add",
      "args": {
        "tick": 0,
        "bpm": 120
      },
      "expect": {
        "replaced": false
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.tempo_map_add",
      "args": {
        "tick": 1920,
        "bpm": 140
      },
      "expect": {
        "replaced": false
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.tempo_map_get",
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.tempo_map_set_active",
      "args": {
        "active": false
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.tempo_map_set_active",
      "args": {
        "active": true
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.tempo_map_remove",
      "args": {
        "tick": 1920
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.tempo_map_clear",
      "expect": {
        "removed": 1
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.seek",
      "args": {
        "ticks": 0
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.play",
      "expect": {
        "playing": true
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.get_state",
      "expect": {
        "paused": false,
        "playing": true
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.seek",
      "args": {
        "ticks": 480
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.stop",
      "expect": {
        "playing": false
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "transport.get_state",
      "expect": {
        "paused": false,
        "playing": false
      },
      "note": "measured ok on the frozen binary"
    }
  ],
  "teardown": "control.quit",
  "notes": "T3 pilot pack, generated from the round-2 measurement of the frozen pilot binary sha256 2eed203147678d3eec7e774d67b555eed95f01bcc96124b423c7c65aa53a7cf6, Zene Studio 0.2.1-alpha.612+a039d26 (probe/measure2.json): every expectation and every refusal kind in this file is a reply that binary actually gave; ids are bound from the replies that created them. The declared 180 s budget on render-class steps is the class bound (IR-15), recorded live in the run ledger."
}
