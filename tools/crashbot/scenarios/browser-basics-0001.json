{
  "id": "browser/basics-0001",
  "scope": "browser",
  "seed": 5000,
  "groups": [
    "browser"
  ],
  "steps": [
    {
      "cmd": "browser.roots",
      "expect": {
        "count": 6,
        "store_loaded": false
      },
      "expect_len": {
        "roots": 6
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "browser.query",
      "expect": {
        "matched": 2193,
        "probed": 0,
        "returned": 25,
        "truncated": false
      },
      "expect_len": {
        "roots": 6
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "browser.tags",
      "expect": {
        "store_loaded": false
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "browser.tag.add",
      "args": {
        "path": "@fixture:agent-control-fixture.mmp",
        "tag": "crashbot"
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "browser.tags",
      "expect": {
        "store_loaded": true
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "browser.query",
      "args": {
        "tags": [
          "crashbot"
        ]
      },
      "expect": {
        "matched": 0,
        "probed": 0,
        "returned": 0,
        "truncated": false
      },
      "expect_len": {
        "roots": 6
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "browser.tag.remove",
      "args": {
        "path": "@fixture:agent-control-fixture.mmp",
        "tag": "crashbot"
      },
      "note": "measured ok on the frozen binary"
    },
    {
      "cmd": "browser.peaks",
      "args": {
        "path": "@work:absent.wav",
        "buckets": 64
      },
      "expect_error": "not_found",
      "note": "measured refusal not_found: @work:absent.wav is not a file (the browser addresses files, not directories)"
    }
  ],
  "teardown": "control.quit",
  "notes": "T3 pilot pack, generated from the round-2 measurement of the frozen pilot binary sha256 2eed203147678d3eec7e774d67b555eed95f01bcc96124b423c7c65aa53a7cf6, Zene Studio 0.2.1-alpha.612+a039d26 (probe/measure2.json): every expectation and every refusal kind in this file is a reply that binary actually gave; ids are bound from the replies that created them. The declared 180 s budget on render-class steps is the class bound (IR-15), recorded live in the run ledger."
}
